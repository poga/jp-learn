{
  "name": "jp-learn",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "test": "node test.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
